#ifndef TRAINING_H
#define TRAINING_H

#include <map>
#include <vector>
#include <string>

using namespace std;

float Training(map<int, pair<int, int>>, string);

void SaveVectors(const string&);

#endif